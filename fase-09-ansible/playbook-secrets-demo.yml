---
- name: Demonstracao de uso do Ansible Vault
  hosts: servidores
  vars_files:
    - secrets.yml

  tasks:
    - name: Mostrar que o segredo foi carregado (sem exibir o valor real)
      debug:
        msg: "Senha do banco tem {{ db_password | length }} caracteres"
